/**
 * Domain layer containing pure business concepts and invariants.
 *
 * <p>This package must not depend on Quarkus, adapters, persistence, SRI, XML,
 * queues, filesystems, HTTP clients, or other infrastructure concerns.
 */
package com.alexastudillo.taxdocument.domain;
